product:
  type: lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - $compose.foundation
  - $compose.material3
  - $compose.runtime

dependencies@jvm:
  - $libs.koin.compose

dependencies@ios:
  - $libs.koin.compose

settings:
  compose:
    version: 1.6.10
    enabled: true
