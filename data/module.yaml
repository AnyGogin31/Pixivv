product:
  type: lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - ../data-remote
  - ../domain
  - $libs.koin.core
  - $libs.kotlinx.datetime

settings:
  android:
    namespace: io.anygogin31.pixivv.data
