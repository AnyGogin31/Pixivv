<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
            android:name="io.anygogin31.pixivv.MainApplication"
            android:allowBackup="false"
            android:dataExtractionRules="@null"
            android:fullBackupContent="@null"
            android:icon="@null"
            android:label="@string/app_name"
            android:memtagMode="async"
            android:roundIcon="@null"
            android:theme="@android:style/Theme.Material.NoActionBar"
            tools:targetApi="31">

        <activity-alias
                android:name="io.anygogin31.pixivv.DefaultIcon"
                android:enabled="true"
                android:exported="true"
                android:targetActivity="io.anygogin31.pixivv.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
                android:name="io.anygogin31.pixivv.MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

    </application>
</manifest>
