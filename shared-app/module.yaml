product:
  type: lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - ../shared-setup
  - $compose.foundation
  - $compose.material3
  - $compose.runtime

settings:
  android:
    namespace: io.anygogin31.pixivv.shared.app
  compose:
    version: 1.6.10
    enabled: true
