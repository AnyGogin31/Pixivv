product:
  type: lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - ../core-remote-constants
  - ../domain
  - $libs.coil
  - $libs.coil.network.ktor

settings:
  android:
    namespace: io.anygogin31.pixivv.core.image.loader
