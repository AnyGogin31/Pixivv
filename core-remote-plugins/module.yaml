product:
  type: lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - ../core-remote-constants
  - $libs.kotlinx.datetime
  - $libs.ktor.client.core
  - $libs.okio

settings:
  android:
    namespace: io.anygogin31.pixivv.core.remote.plugins
